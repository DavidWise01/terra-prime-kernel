# Aeonic Coherence Kernel v0.1

Package contents:

- `SPEC.md` — kernel specification
- `prototype.py` — runnable reference prototype
- `manifest.json` — machine-readable manifest
- `index.html` — visual overview

Run:

```bash
python prototype.py
```

Expected final demo behavior:

- final stability around adaptive band
- warning stays gentle
- observer alerts remain false
- hash ledger produces final hash
